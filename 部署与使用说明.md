# 豆包网页反代 - 部署与使用说明

基于 [lumingya/universal-web-api](https://github.com/lumingya/universal-web-api)(浏览器接管式:真实浏览器里登录豆包网页,对外提供 OpenAI 兼容 API)。
本说明对应 fork 仓库 **`Karmel5950/universal-web-api`** 的 `local/doubao-workflow-fix` 分支,**包含两个上游没有的关键修复**,必须用这个分支部署:

| 修复 | 解决的问题 |
|------|-----------|
| 解析器从 SSE 流提取生成图片 URL | 豆包新版页面消息区在 Shadow DOM 里,上游的 DOM 图片提取失效,API 生图返回空 |
| "新对话"按钮 XPath 选择器 + 去掉 ctrl+k | 上游选择器在当前版豆包页面全部失效;ctrl+k 会弹搜索栏 |

## 一、环境要求

- Windows 10/11(上游对 Windows 支持最完整,Linux/macOS 核心功能可用但未充分测试)
- [uv](https://docs.astral.sh/uv/getting-started/installation/)(PowerShell 安装:`powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"`)
- Chrome 或 Edge(一般自带)
- 豆包账号一个(手机号或抖音扫码)

## 二、部署步骤

```powershell
# 1. 克隆(注意分支)
git clone -b local/doubao-workflow-fix https://github.com/Karmel5950/universal-web-api.git
cd universal-web-api

# 2. 用 uv 建环境装依赖(venv 目录名必须是 venv,start.py 会复用它)
uv venv venv --python 3.12
uv pip install -r requirements.txt --python venv/Scripts/python.exe

# 3. 写 .env(见下方完整内容)

# 4. 启动(首次会自动拉起受控浏览器)
venv\Scripts\python.exe start.py
```

`.env` 完整内容(单人本机用法,只监听本机、无认证、直连):

```ini
CURRENT_VERSION=1.01
AUTO_UPDATE_ENABLED=false
APP_HOST=127.0.0.1
APP_PORT=8199
APP_DEBUG=false
LOG_LEVEL=INFO
AUTH_ENABLED=false
CORS_ENABLED=true
CORS_ORIGINS=*
BROWSER_PORT=9222
BROWSER_PATH=C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
DASHBOARD_ENABLED=true
DASHBOARD_FILE=static/index.html
PUBLIC_BASE_URL=http://127.0.0.1:8199
PROXY_ENABLED=false
SITES_CONFIG_FILE=config/sites.json
```

> `BROWSER_PATH` 按实际浏览器路径改;想用 Chrome 就指向 chrome.exe。

## 三、登录豆包(只做一次)

首次启动会弹出一个**新的浏览器窗口**(独立配置,和你日常用的浏览器互不相干)。在这个窗口里打开 `doubao.com` 并登录(手机号验证码/抖音扫码)。登录态永久保存在项目目录的 `chrome_profile/` 里,之后启动不用再登。**日常使用别关这个窗口**,最小化即可。

## 四、使用(一条命令生图)

把 `gen.py` 放在任意目录,用系统 Python 或 venv Python 跑均可(纯标准库,无依赖):

```powershell
python gen.py "一只戴宇航员头盔的柴犬,可爱风格"
python gen.py "赛博朋克城市夜景" --out D:\pics
```

输出:

```
[+] 生成完成,耗时 23.5s
[+] 已保存(原图, 4514KB): D:\pics\20260908_221644_2.png
...
```

也可以直接调 API(OpenAI 兼容):

```powershell
curl http://127.0.0.1:8199/v1/chat/completions -H "Content-Type: application/json" -d @img_req.json
```

要点:

- **`preset_name: "文本-图像预设"` 必带**,否则可能走"朗读"预设,返回的是音频不是图
- 响应 `media[]` 是图片列表(服务自动下载过一份,在 `universal-web-api/download_images/`);`gen.py` 会另存一份到你指定的目录
- 豆包单次可能生成 1~4 张,图片右下角带"豆包AI生成"水印(网页版行为)
- 单次耗时 25~60 秒
- Git Bash 里 curl 内联中文 JSON 会转义出错,用 `--data-binary @文件` 或 PowerShell

## 五、日常启停

- 启动:`venv\Scripts\python.exe start.py`(一条命令,自动拉起受控浏览器 + API 服务)
- 如果浏览器已在跑而服务没跑:`venv\Scripts\python.exe main.py`
- 控制面板:`http://127.0.0.1:8199/`(状态、日志、请求历史)
- 关机重启后按上面顺序再来即可,登录态不丢

## 六、故障排查

| 现象 | 处理 |
|------|------|
| 返回空内容 / 提示未登录 | 受控浏览器里豆包登录态失效,重新登录一次 |
| 弹验证码 | 在受控浏览器里人肉点掉即可;若频繁出现,删掉 `chrome_profile/` 目录重新扫码,并把机器挪回家用网络跑(机房 IP/高频调用是风控重点) |
| 生图返回 media 为空 | 确认用的分支对不对(必须 `local/doubao-workflow-fix`);确认 preset_name 没写错 |
| 端口被占 | 改 `.env` 的 `APP_PORT` / `BROWSER_PORT` |
| git pull 升级后生图又返回空 | 上游更新覆盖了解析器补丁,从 `local/doubao-workflow-fix` 分支重新合并;上游若合并了 [PR #26](https://github.com/lumingya/universal-web-api/pull/26) 则不再需要 |

## 七、安全边界

- `.env` 默认只绑 `127.0.0.1` 且无认证,**不要改成 0.0.0.0 暴露到局域网**;确需外部访问,先设 `AUTH_ENABLED=true` + `AUTH_TOKEN`
- 逆向网页接口仅供个人学习,限个人低频使用;豆包不支持 Function Calling,别拿它当编程 Agent 后端
